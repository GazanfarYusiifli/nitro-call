# .env.example — committed to git so Vercel/Render know which env vars are needed
# NEVER commit the real .env file with secrets

# The public URL of your deployed backend on Render
VITE_BACKEND_URL=https://your-app-name.onrender.com
